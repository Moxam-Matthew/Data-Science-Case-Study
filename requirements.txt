# Core
pandas>=2.0
numpy>=1.24
scipy>=1.10
matplotlib>=3.7

# Modelling
# statsmodels, not sklearn, for the interpretable logistic regression:
# sklearn.LogisticRegression applies L2 by default (penalty='l2', C=1.0), so its
# coefficients are penalised rather than maximum-likelihood, and it exposes no
# standard errors. Clinical reporting needs odds ratios with 95% CIs.
statsmodels>=0.14
# sklearn is used for Pipeline/ColumnTransformer (imputation inside CV folds),
# cross-validation, metrics, and IterativeImputer (MICE).
scikit-learn>=1.3
xgboost>=2.0

# Survival / competing risks
lifelines>=0.27

# Reproducible data access (UCI id=880) so no row-level data is committed
ucimlrepo>=0.0.7
